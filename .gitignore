.DS_Store
node_modules
/dist


# local env files
.env

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

uploads/*

# local DB snapshots taken by src/scripts/backup-user-merge.ts — real user data
backups/

# one-off broadcast ledgers (src/scripts/announce.ts), one per campaign
announce-*-sent.jsonl

# Firebase admin service account private key. NEVER commit this — an earlier
# copy sat in this repo's public history from 2023-05-15 until it was purged,
# and the key had to be revoked. Provision it out of band on each machine.
fcm.json